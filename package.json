{
  "name": "slack_football_poll",
  "version": "1.0.0",
  "description": "",
  "main": "poll.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lambda-dev": "AWS_LAMBDA_JS_RUNTIME=nodejs8.10 netlify-lambda serve ./functions -c ./webpack.config.js",
    "lambda-build": "AWS_LAMBDA_JS_RUNTIME=nodejs8.10 netlify-lambda build ./functions -c ./webpack.config.js",
    "build": "npm run lambda-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.1.2",
    "webpack-node-externals": "^1.7.2"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.15"
  }
}
