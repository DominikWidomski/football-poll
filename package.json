{
  "name": "slack_football_poll",
  "version": "1.0.0",
  "description": "",
  "main": "poll.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lambda-dev": "netlify-lambda serve ./functions",
    "lambda-build": "netlify-lambda build ./functions",
    "build": "npm run lambda-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {
    "netlify-lambda": "^0.4.0"
  }
}
